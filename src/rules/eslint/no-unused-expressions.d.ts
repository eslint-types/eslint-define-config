export interface Schema0 {
  allowShortCircuit?: boolean;
  allowTernary?: boolean;
  allowTaggedTemplates?: boolean;
  enforceForJSX?: boolean;
}

export type NoUnusedExpressionsRuleOptions = [Schema0?];
