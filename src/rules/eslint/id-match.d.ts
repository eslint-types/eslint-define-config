export type Schema0 = string;

export interface Schema1 {
  properties?: boolean;
  classFields?: boolean;
  onlyDeclarations?: boolean;
  ignoreDestructuring?: boolean;
}

export type IdMatchRuleOptions = [Schema0?, Schema1?];
